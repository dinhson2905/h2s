<%= form_with(model: [ @set_card, @set_card.flask_cards.build ], local: true) do |form| %>
  <div class="form-group">
    <label for=""> Kanji: </label>
    <%= form.text_area :cach_viet, class: "form-control" %>
  </div>
  <div class="form-group">
    <label for=""> hiragana: </label>
    <%= form.text_area :cach_doc, class: "form-control" %>
  </div>
  <div class="form-group">
    <label for=""> Nghĩa: </label>
    <%= form.text_area :nghia, class: "form-control" %>
  </div>
  <%= form.submit :class => "btn btn-primary" %>
<% end %>
