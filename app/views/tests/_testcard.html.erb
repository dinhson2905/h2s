<% if index == 0 %>
     <div class="testcard item active" >
     <div class="card-hira hira-flash">

     <div class="cach_viet">
          <%= t.cachviet %>
     </div>
     <div class="cach_doc">
          <%= t.cachdoc %>
     </div>
     </div>
     <%# <%= link_to 'xóa', method: :delete, data: { confirm: 'Are you sure?' } %>
     </div>

<% else %>
<div class="testcard item " >
<div class="card-hira hira-flash">

<div class="cach_viet">
<%= t.cachviet %>
</div>
<div class="cach_doc">
<%= t.cachdoc %>
</div>
</div>
     <%# <%= link_to 'xóa', method: :delete, data: { confirm: 'Are you sure?' } %>
</div>

<% end %>

